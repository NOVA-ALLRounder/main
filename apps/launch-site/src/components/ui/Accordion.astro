---
export interface Props {
  items: { question: string; answer: string }[];
}

const { items } = Astro.props;
---

<div class="divide-y divide-neutral-200 border-t border-b border-neutral-200">
  {items.map((item, index) => (
    <details class="group" id={`faq-${index}`}>
      <summary class="flex items-center justify-between gap-4 py-5 cursor-pointer list-none text-left hover:text-neutral-600 transition-colors [&::-webkit-details-marker]:hidden">
        <span class="text-base font-medium text-neutral-900 group-open:text-accent-600 transition-colors">
          {item.question}
        </span>
        <span class="flex-shrink-0 w-5 h-5 text-neutral-400 group-open:rotate-45 transition-transform duration-200" aria-hidden="true">
          <svg viewBox="0 0 20 20" fill="currentColor">
            <path d="M10 5a1 1 0 011 1v3h3a1 1 0 110 2h-3v3a1 1 0 11-2 0v-3H6a1 1 0 110-2h3V6a1 1 0 011-1z" />
          </svg>
        </span>
      </summary>
      <div class="pb-5 text-neutral-500 text-body leading-relaxed">
        <p>{item.answer}</p>
      </div>
    </details>
  ))}
</div>
