---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Section from '../components/ui/Section.astro';
import Container from '../components/ui/Container.astro';
import Button from '../components/ui/Button.astro';
import StepCard from '../components/ui/StepCard.astro';
import Accordion from '../components/ui/Accordion.astro';

const faqItems = [
  { question: 'NOVA는 어떤 문제를 해결하나요?', answer: '반복적인 업무 프로세스를 지능적으로 자동화하여, 팀이 창의적이고 전략적인 일에 집중할 수 있도록 돕습니다. 데이터 입력부터 의사결정 지원까지 폭넓게 활용됩니다.' },
  { question: '도입에 얼마나 걸리나요?', answer: '대부분의 팀이 30분 이내에 첫 자동화를 구성합니다. 복잡한 엔터프라이즈 환경도 2주 이내 파일럿이 가능합니다.' },
  { question: '기존 시스템과 연동되나요?', answer: 'REST API, Webhook, 주요 SaaS 도구(Slack, Notion, Google Workspace 등)와 기본 연동을 지원합니다. 커스텀 연동도 API를 통해 구현할 수 있습니다.' },
  { question: '데이터 보안은 어떻게 처리되나요?', answer: '모든 데이터는 전송 시 TLS 1.3, 저장 시 AES-256으로 암호화됩니다. SOC 2 Type II 인증을 준비 중이며, 데이터 처리 위치를 선택할 수 있습니다.' },
  { question: '무료 플랜이 있나요?', answer: '네, 개인 사용자를 위한 무료 플랜을 제공합니다. 월 1,000건의 자동화 실행과 핵심 기능을 무제한으로 사용할 수 있습니다.' },
  { question: '팀 규모에 제한이 있나요?', answer: '없습니다. 1인 사용자부터 수천 명 규모의 조직까지 유연하게 확장됩니다. 엔터프라이즈 플랜에서는 맞춤 구성이 가능합니다.' },
];
---

<BaseLayout title="NOVA — 일의 흐름을 바꾸는 지능형 자동화">
  <Header slot="header" />

  <!-- Hero -->
  <Section padding="large" class="relative overflow-hidden bg-[#05081a] text-white">
    <div aria-hidden="true" class="absolute inset-0">
      <div class="absolute -top-32 left-1/2 h-96 w-[56rem] -translate-x-1/2 rounded-full bg-indigo-500/30 blur-3xl"></div>
      <div class="absolute bottom-0 left-1/2 h-80 w-[44rem] -translate-x-1/2 rounded-full bg-blue-400/25 blur-3xl"></div>
    </div>
    <Container>
      <div class="relative text-center max-w-4xl mx-auto animate-on-scroll">
        <p class="text-caption text-blue-200/80 font-medium tracking-wide uppercase mb-4">NOVA Platform</p>
        <h1 class="text-h1 md:text-display text-white mb-6 text-balance">
          The Future of Work<br class="hidden md:block" /> Happens in One Flow
        </h1>
        <p class="text-body-lg text-white/75 max-w-2xl mx-auto mb-10">
          B2C와 B2B 모두를 위한 지능형 자동화. 개인의 생산성부터 기업 운영 체계까지, 하나의 플랫폼에서 연결하고 실행합니다.
        </p>

        <div class="max-w-3xl mx-auto grid grid-cols-1 md:grid-cols-[1fr_auto_auto] gap-3 mb-8">
          <label for="hero-email" class="sr-only">이메일</label>
          <input
            id="hero-email"
            type="email"
            placeholder="Enter your email"
            class="w-full rounded-xl border border-white/25 bg-white/95 px-4 py-3.5 text-neutral-900 placeholder:text-neutral-500 focus:outline-none focus:ring-2 focus:ring-emerald-400/50"
          />
          <Button variant="primary" size="lg" href="/docs/getting-started" class="bg-emerald-500 hover:bg-emerald-400 whitespace-nowrap">
            시작하기
          </Button>
          <Button variant="secondary" size="lg" href="/product" class="bg-transparent border-white/40 text-white hover:bg-white/10 whitespace-nowrap">
            데모 보기
          </Button>
        </div>

        <div class="flex flex-wrap items-center justify-center gap-3 text-xs text-white/60">
          <span class="rounded-full border border-white/20 px-3 py-1">No credit card</span>
          <span class="rounded-full border border-white/20 px-3 py-1">B2C + B2B Ready</span>
          <span class="rounded-full border border-white/20 px-3 py-1">Secure by Design</span>
        </div>
      </div>
    </Container>
  </Section>

  <!-- Social Proof -->
  <Section class="bg-[#090f2b]">
    <Container>
      <div class="animate-on-scroll">
        <p class="text-center text-caption text-white/55 font-medium tracking-wide uppercase mb-10">
          Trusted by teams of every size
        </p>
        <div class="flex flex-wrap items-center justify-center gap-8 md:gap-16 mb-16">
          {['Company A', 'Company B', 'Company C', 'Company D', 'Company E'].map((name) => (
            <div class="flex items-center justify-center h-8 px-4 text-white/35 text-sm font-semibold tracking-wider uppercase">
              {name}
            </div>
          ))}
        </div>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-3xl mx-auto">
          {[
            { value: '10,000+', label: '활성 사용자' },
            { value: '5M+', label: '월간 자동화 처리 건수' },
            { value: '99.9%', label: '업타임 SLA' },
          ].map((stat) => (
            <div class="text-center rounded-2xl border border-white/10 bg-white/5 py-8">
              <p class="text-h2 text-white font-bold">{stat.value}</p>
              <p class="text-caption text-white/65 mt-1">{stat.label}</p>
            </div>
          ))}
        </div>
      </div>
    </Container>
  </Section>

  <!-- How it Works -->
  <Section>
    <Container>
      <div class="text-center mb-16 animate-on-scroll">
        <h2 class="text-h2 text-neutral-900 mb-4">세 단계로 시작하세요</h2>
        <p class="text-body-lg text-neutral-500 max-w-xl mx-auto">복잡한 설정 없이, 몇 분 안에 자동화의 효과를 경험합니다.</p>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-12 animate-on-scroll">
        <StepCard step={1} title="연결" description="기존 도구와 데이터 소스를 간단히 연결합니다. 별도 코딩이 필요 없습니다." />
        <StepCard step={2} title="자동화 설계" description="직관적인 에디터로 워크플로우를 구성합니다. AI가 최적의 흐름을 제안합니다." />
        <StepCard step={3} title="실행 & 모니터링" description="실시간 대시보드에서 자동화 성과를 확인하고, 필요 시 즉시 조정합니다." />
      </div>
    </Container>
  </Section>

  <!-- Benefits Section 1 -->
  <Section background="light">
    <Container>
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center animate-on-scroll">
        <div>
          <p class="text-caption text-accent-600 font-medium tracking-wide uppercase mb-3">의사결정 가속</p>
          <h2 class="text-h2 text-neutral-900 mb-6">데이터에서 인사이트까지,<br />몇 시간이 아닌 몇 초</h2>
          <p class="text-body-lg text-neutral-500 mb-8">
            흩어진 데이터를 자동으로 수집·분석하고, 핵심 지표만 정제하여 전달합니다. 더 이상 스프레드시트를 뒤질 필요가 없습니다.
          </p>
          <ul class="space-y-3">
            {['실시간 데이터 통합 및 시각화', '이상 징후 자동 감지 및 알림', '맞춤형 리포트 자동 생성'].map((item) => (
              <li class="flex items-start gap-3 text-body text-neutral-600">
                <svg class="w-5 h-5 text-accent-500 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" /></svg>
                <span>{item}</span>
              </li>
            ))}
          </ul>
        </div>
        <div class="bg-neutral-200 rounded-2xl aspect-[4/3] flex items-center justify-center">
          <span class="text-neutral-400 text-sm">이미지 플레이스홀더</span>
        </div>
      </div>
    </Container>
  </Section>

  <!-- Benefits Section 2 -->
  <Section>
    <Container>
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center animate-on-scroll">
        <div class="order-2 lg:order-1 bg-neutral-200 rounded-2xl aspect-[4/3] flex items-center justify-center">
          <span class="text-neutral-400 text-sm">이미지 플레이스홀더</span>
        </div>
        <div class="order-1 lg:order-2">
          <p class="text-caption text-accent-600 font-medium tracking-wide uppercase mb-3">오류 제로에 가깝게</p>
          <h2 class="text-h2 text-neutral-900 mb-6">사람의 실수를<br />시스템이 방지합니다</h2>
          <p class="text-body-lg text-neutral-500 mb-8">
            반복 작업에서 발생하는 입력 오류, 누락, 지연을 구조적으로 제거합니다. 규칙 기반 검증과 AI 보정이 동시에 작동합니다.
          </p>
          <ul class="space-y-3">
            {['자동 데이터 검증 및 이상치 보정', '프로세스 병목 자동 감지', '규정 준수 자동 체크리스트'].map((item) => (
              <li class="flex items-start gap-3 text-body text-neutral-600">
                <svg class="w-5 h-5 text-accent-500 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" /></svg>
                <span>{item}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </Container>
  </Section>

  <!-- B2C Block -->
  <Section background="light">
    <Container>
      <div class="max-w-3xl mx-auto text-center animate-on-scroll">
        <p class="text-caption text-accent-600 font-medium tracking-wide uppercase mb-3">개인 사용자</p>
        <h2 class="text-h2 text-neutral-900 mb-6">당신의 시간을 되돌려 드립니다</h2>
        <p class="text-body-lg text-neutral-500 mb-10">
          매일 반복하는 정리, 분류, 보고 작업을 자동화하세요. 더 중요한 일에 집중할 여유가 생깁니다.
        </p>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 mb-10 text-left">
          {[
            { title: '자동 정리', desc: '파일, 메일, 일정을 규칙에 따라 자동 분류' },
            { title: '스마트 요약', desc: '긴 문서와 스레드를 핵심만 추출하여 요약' },
            { title: '반복 자동화', desc: '주기적 작업을 한 번 설정으로 영구 자동 실행' },
          ].map((item) => (
            <div class="bg-white rounded-xl p-6 border border-neutral-200">
              <h3 class="text-h4 text-neutral-900 mb-2">{item.title}</h3>
              <p class="text-caption text-neutral-500">{item.desc}</p>
            </div>
          ))}
        </div>
        <Button variant="primary" size="lg" href="/pricing">무료로 시작하기</Button>
      </div>
    </Container>
  </Section>

  <!-- B2B Block -->
  <Section>
    <Container>
      <div class="max-w-3xl mx-auto text-center animate-on-scroll">
        <p class="text-caption text-accent-600 font-medium tracking-wide uppercase mb-3">기업 & 팀</p>
        <h2 class="text-h2 text-neutral-900 mb-6">조직의 규모에 맞게<br />확장됩니다</h2>
        <p class="text-body-lg text-neutral-500 mb-10">
          IT 관리자가 안심할 수 있는 보안, 감사 로그, 세밀한 권한 관리. 대규모 팀에 필요한 모든 것이 준비되어 있습니다.
        </p>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 mb-10 text-left">
          {[
            { title: 'SSO & RBAC', desc: '기업 인증 체계와 역할 기반 접근 제어를 기본 지원' },
            { title: '감사 로그', desc: '모든 자동화 실행 이력을 추적하고 보고서로 내보내기' },
            { title: '전담 지원', desc: '도입부터 운영까지 전담 매니저와 SLA 기반 기술 지원' },
          ].map((item) => (
            <div class="bg-neutral-50 rounded-xl p-6 border border-neutral-200">
              <h3 class="text-h4 text-neutral-900 mb-2">{item.title}</h3>
              <p class="text-caption text-neutral-500">{item.desc}</p>
            </div>
          ))}
        </div>
        <Button variant="primary" size="lg" href="/solutions">영업팀에 문의하기</Button>
      </div>
    </Container>
  </Section>

  <!-- FAQ -->
  <Section background="light">
    <Container narrow>
      <div class="animate-on-scroll">
        <div class="text-center mb-12">
          <h2 class="text-h2 text-neutral-900 mb-4">자주 묻는 질문</h2>
          <p class="text-body-lg text-neutral-500">궁금한 점이 있으신가요? 가장 많이 받는 질문을 정리했습니다.</p>
        </div>
        <Accordion items={faqItems} />
      </div>
    </Container>
  </Section>

  <!-- Bottom CTA Banner -->
  <Section background="dark">
    <Container>
      <div class="text-center animate-on-scroll">
        <h2 class="text-h2 text-white mb-4">지금 바로 시작하세요</h2>
        <p class="text-body-lg text-neutral-400 max-w-xl mx-auto mb-10">
          무료 플랜으로 시작하고, 팀의 성장에 맞춰 확장하세요. 신용카드 없이 바로 사용할 수 있습니다.
        </p>
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
          <Button variant="primary" size="lg" href="/docs/getting-started" class="bg-white text-neutral-900 hover:bg-neutral-100">무료로 시작하기</Button>
          <Button variant="ghost" size="lg" href="/solutions" class="text-neutral-300 hover:text-white hover:bg-white/10">영업팀에 연락하기</Button>
        </div>
      </div>
    </Container>
  </Section>

  <Footer slot="footer" />
</BaseLayout>
