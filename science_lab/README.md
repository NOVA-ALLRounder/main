# Science Lab - 자율 과학 발견 시스템

AI 기반 자율 과학 발견 및 가상 실험 시스템입니다.

## 📋 기능

- **의도 분류**: 가설 vs 질문 자동 분류
- **문헌 검색**: ArXiv, Semantic Scholar API 연동
- **독창성 평가**: 기존 연구와의 유사도 분석
- **3가지 방법론 제안**: Analytical, Simulation, Data-Driven
- **가상 실험**: 자동 코드 생성 및 실행
- **보고서 생성**: IMRAD 형식 학술 보고서

## 🚀 설치 및 실행

```bash
# 의존성 설치
pip install -r requirements.txt

# 환경변수 설정
cp .env.example .env
# .env 파일에 OPENAI_API_KEY 입력

# 서버 실행
python api.py
```

브라우저에서 `http://localhost:8000` 접속

## 🧪 테스트

```bash
python -m pytest tests/ -v
```

## 📁 프로젝트 구조

```
science_lab/
├── agents/         # 6개 에이전트 모듈
│   ├── router.py   # 의도 분류
│   ├── librarian.py# 문헌 검색
│   ├── pi.py       # 연구책임자 (독창성/방법론)
│   ├── engineer.py # 코드 생성/실험
│   ├── critic.py   # 검토
│   └── author.py   # 보고서 작성
├── tools/          # 도구 모듈
│   ├── arxiv_search.py
│   ├── semantic_scholar.py
│   └── code_executor.py
├── web/            # 프론트엔드
│   ├── index.html
│   ├── index.css
│   └── app.js
├── tests/          # 테스트
├── api.py          # FastAPI 백엔드
├── workflow.py     # LangGraph 워크플로우
├── state.py        # 상태 정의
├── config.py       # 설정
└── database.py     # DB 연동
```

## 📖 사용법

1. **가설 입력**: "카페인 섭취가 집중력을 향상시킬 것이다"
2. **분류 결과 확인**: Intent, 독창성 점수
3. **방법론 선택**: 3가지 중 하나 선택
4. **실험 실행**: 자동 코드 생성 및 실행
5. **보고서 확인**: IMRAD 형식 결과 보고서
